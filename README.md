# λ (Lambda)
A C++ Spectral Path Tracer using my library numli

## Features
* Produces a pure black image still... fixing all the bugs should produce images with direct lighting by next event estimation. Should...

## Future Work
* An image that isn't black.

## Dependencies
This project uses `C++23` and has been tested with `g++15`.